<% return if current_user == @user %>
<div id="follow_form">
  <% if current_user.following? user %>
    <%= render "unfollow", follow_user: follow_user, user: @user %>
  <% else %>
    <%= render "follow", follow_user: follow_user, user: @user %>
  <% end %>
</div>
